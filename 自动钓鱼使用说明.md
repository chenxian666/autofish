# 自动钓鱼使用说明（autofish）

一个独立的挂机脚本，用**系统音频**判断是不是自己的鱼上钩，然后自动完成收鱼抛竿。
不截屏、不读游戏内存、不注入游戏进程、不装驱动（见 `docs/adr/0001`）。

---

## 一、准备

需要一个带图形界面（Tkinter）的 Python 环境，依赖 `numpy` 和 `soundcard`。

```
pip install numpy soundcard
```

**最省事的启动方式：双击 `AutoFish.exe`。**

这个 exe 是打好的包（约 25 MB），**双击直接出界面，没有任何命令行黑窗口**，
也不依赖 Python 环境 —— 拷到别的电脑也能用。启动比脚本慢 1～3 秒（单文件包要先解包）。

> 想自己改代码、或者要命令行参数，还是用下面的 `.bat`。
> exe 和 `.bat` **共用同一份配置和标定结果**（都在 `%LOCALAPPDATA%\AutoFish\`），随便换着用，
> 不用重新标定。打包步骤见 `打包成exe.md`。

<details>
<summary>不喜欢 exe 的话，原来的批处理方式仍然可用</summary>

双击 `autofish.bat` —— 会弹一个命令行黑窗口，功能完全一样。

</details>

> ### ⚠️ 会弹 UAC，请点「是」
>
> 三角洲行动因为自带反作弊（ACE），是**以管理员权限运行**的
> （实测：窗口标题 `三角洲行动  `，进程 `DeltaForceClient-Win64-Shipping.exe`，已提权 = True）。
>
> Windows 有一条硬规矩（UIPI）：**普通权限的程序不能向管理员权限的窗口注入点击**，
> 而且是**静默丢弃** —— 不报错、不弹窗。你只会看到"脚本在跑、日志也写了收鱼，但鱼还是跑掉"。
>
> 所以 exe 里内嵌了"需要管理员权限"的清单、`autofish.bat` 会自己请求提权，
> 弹 UAC 时点「是」即可。点「否」的话脚本照样能识别到鱼，但点击一定不生效 ——
> 日志里会明确写出来。

或者手动：

```
AutoFish.exe                  图形界面（无黑窗口）
autofish.bat                  图形界面（带控制台窗口）
autofish.bat --nogui          纯控制台
autofish.bat --calibrate 120  标定：只听不点，跑 120 秒
autofish.bat --list           列出音频设备
```

`--list`、`--calibrate` 这类要看控制台输出的参数在 exe 里没有意义（没有控制台可看），
需要的时候用 `.bat`；标定在界面里点「标定…」就行。

---

## 二、第一次用，按这个顺序

**1. 先标定**（重要，别跳过）

点界面上的「标定…」，填 120 秒，然后正常钓鱼。这段时间**只听不点**。
标定结束后会算出建议门限并自动填进输入框，确认无误就点「应用门限」。

> 标定到底在算什么、为什么不能跳过，见**第九节**。

**2. 检查三件事**

- 「设备」要显示 `扬声器 (FxSound Audio Enhancer)`（默认输出设备的回环）
- 「前台」显示 OK —— 前台窗口按**标题含「三角洲行动」或进程是三角洲**来认（任一命中即可）
- 「光标」显示 OK —— 鼠标要停在游戏窗口内

**3. 把鼠标停在钓鱼 UI 上，然后就别动了**

点击打在**光标当前位置**（脚本不移动鼠标）。所以鼠标必须一直悬在需要点的地方。

**4. 按 F9（或点「开始挂机」）**

---

## 三、三个门限分别在干什么

理解这三个数字，你就知道出问题该拧哪个。

| 门限 | 默认 | 它回答的问题 | 调太紧的后果 | 调太松的后果 |
|---|---|---|---|---|
| **归一分 ≥** | 0.80 | 音频里**有没有**咬钩音 | 听漏，自己的鱼也不点 | 什么声音都可能算咬钩 |
| **电平 ≥ dBFS** | −28.0 | 这个咬钩音**离你多近**（下限） | 自己的鱼被误判成别人的 | 远处的人也能骗过 |
| **电平 ≤ dBFS** | 0（关） | 这个咬钩音**会不会太响**（上限） | 自己的鱼被当"太响"拦掉 | 贴脸的枪/爆炸也能骗过 |
| **\|声像差\| ≤ dB** | 5.0 | 这个咬钩音**在不在你正前方** | 视角稍偏就点不到 | 侧面来的人也能骗过 |

- **电平**是主力判据，现在可以设成**区间**：`电平 ≥`（下限）和 `电平 ≤`（上限）之间才算自己的鱼。
  你自己的浮漂就在跟前，别人的在远处会被距离衰减打下去 —— 这是下限拦的；
  **上限拦的是"太响"**：贴脸的敌方脚步、枪声、爆炸往往比你的浮漂咬钩音更响，
  设个上限能把它们一并拦掉。**上限填 0 就是关闭上限**（默认）。
- **声像差**是辅助筛子。你自己的浮漂在视线正中 ⇒ 声像差 ≈ 0；别人在侧后方 ⇒ 声像差明显。
- **归一分**不是归属判据。它是归一化的，对音量大小和左右方位**都不敏感**，所以只能回答"有没有"（见 `docs/adr/0002`）。

> 电平区间怎么定？**看日志里你自己的鱼的电平**。比如你观察到自己的鱼基本都在 −24 附近，
> 就设 `电平 ≥ −26` + `电平 ≤ −22`（一个 4 dB 的窄区间），既拦远处也拦太响。这也是本文档作者实测后采用的默认区间。

### 实测参考值

把参考音效直接播出去、再回环录回来（相当于"自己的鱼"的最佳情形）：

| 指标 | 值 |
| --- | --- |
| 归一分 | **0.989** |
| 电平 | **−25.9 dBFS** |
| 声像差 | **+0.0 dB** |
| 双声道相干 | 1.000 |

也就是默认的电平门限 −28 dBFS 相对"满音量播放"（实测 −25.9 dBFS）留了约 2 dB 余量。
如果你游戏里音效音量调得低，实测电平会更低，这时**必须靠标定**把门限放宽。

---

## 四、兜底与已知限制

**兜底补杆**：20 秒内**一次都没触发**（触发 = 真的点了鼠标），就无条件点一次重新抛竿。
注意它按"没触发"算，不是按"没命中"算 —— 否则周围一直有人上钩时，你自己的空竿永远等不到兜底。

**已知限制**（都是刻意接受的取舍）：

1. 别人正好站在你**视线正前方**时，声像差同样≈0，会被误判成你的。无解。
2. 不做延迟仲裁（点击窗口太紧，等一下鱼就跑了），所以人多时可能连续误触发。见 `docs/adr/0003`。
3. 你自己的浮漂如果不在视线正前方（比如你转了视角），会被声像差筛掉。**运行期间别转视角。**
4. 兜底补杆本身不看任何判据，是故意留的"防卡死"手段。不需要就把「兜底 s」设成 0。

---

## 五、故障排查

| 现象 | 先查这个 |
| --- | --- |
| 一次都不点 | 看「电平」实时值是不是**低于下限或高于上限**；对照 `hits.csv` 里的 `level_db` 分布，把区间放宽 |
| 一直误点 | 收紧「电平」区间（抬高下限、压低上限）；对照 `hits\` 里的录音听一遍，把不属于自己的那些的电平找出来 |
| 「前台不符」 | 前台窗口不是游戏（按标题「三角洲行动」或进程 deltaforce 判），看日志里的具体提示 |
| 「光标不在游戏内」 | 鼠标不在游戏窗口上；不需要这项保护就把 `require_cursor_in_game` 改成 `false` |
| 完全没反应、分数恒为 0 | 用 `--list` 确认设备；`--calibrate 30` 看能不能听到东西 |
| **识别到了、日志也写了收鱼，但鱼还是跑** | **脚本没跑在管理员权限下**。看日志里有没有「⚠ 点击没有生效」或「游戏进程以管理员身份运行」。双击 `autofish.bat` 并在 UAC 弹窗点「是」 |
| 想确认三道闸门有没有生效 | 播 `测试音频\` 里的素材，不用开游戏，见那个文件夹的 `说明.md` |
| 声音卡顿/断续 | 关掉别的占用音频的程序 |

### 「前台窗口或光标不在游戏内」是什么意思

这是**最后一道保险**。完整的判定顺序是：

```
电平 → 声像差 → 前台窗口
```

三道全过才会点鼠标。所以这句提示的意思是：
**前两道（电平和声像差）都已经通过了，但游戏不在前台 —— 所以不点。**

判定时同时要求两件事（**标题或进程名，任一命中即算**）：

1. **前台窗口**：标题含「三角洲行动」，**或**进程是 `DeltaForceClient-Win64-Shipping.exe`
   （配置键 `foreground_title` / `foreground_process`，进程名是子串匹配、不分大小写）。
   只看标题不够稳 —— 标题可能为空或被改；进程名是更硬的锚点。
2. **光标所在窗口**：同样按上面两条匹配（可用 `require_cursor_in_game: false` 关掉这条）

| 情况 | 提示 |
| --- | --- |
| 游戏不在前台（比如你在看浏览器） | 「前台窗口「xxx」（进程 yyy）不是游戏…」 |
| 游戏在前台，但光标在别的窗口/别的显示器上 | 「前台窗口没问题，但光标停在「xxx」上」 |
| 标题和进程都认不出游戏 | 一直提示第一句，改配置里的 `foreground_title` / `foreground_process` |

**对，只要没在玩三角洲，它就会一直提示这个** —— 这是设计如此，不是故障。

反过来说，**跑测试音频时看到这句恰恰是好消息**：它证明电平和声像两道闸门都判通过了，
而且因为窗口不匹配，它**绝不会真的点鼠标**。这就是"不开游戏零风险测试"的原理。

脚本会把实际的前台窗口标题**和进程名**打进日志，比如：

```
前台窗口「WorkBuddy」（进程 WorkBuddy.exe）不是游戏（要含「三角洲行动」或进程「deltaforce」），因此不会点击。
```

一眼就能判断是卡在窗口上，还是卡在别的判据上。界面顶部那行
「前台 OK / 不符」「光标 OK / 不在游戏内」也是实时状态。

> **历史事故（2026-09-19）**：配置里的 `foreground_title` 曾被测试脚本污染成
> `__NO_SUCH__`，导致游戏在前台也一直判「不在游戏」。已还原并加了进程名兜底；
> 测试脚本也改成了「测前备份配置文件、测后整体还原」，不会再污染。
> 如果你哪天看到标题又变成奇怪的值，检查 `autofish.json` 第 10 行即可。

> **例外**：如果你用**独占全屏**，某些游戏下 `WindowFromPoint` 拿不到游戏自己的窗口，
> 会出现"明明在游戏里却一直提示光标不在游戏内"。这种情况把配置里的
> `require_cursor_in_game` 改成 `false` 即可 —— 前台窗口那道检查已经足够安全了。

---

## 六、文件都在哪

| 内容 | 路径 |
| --- | --- |
| 脚本 | `autofish.py`、`autofish.bat` |
| 测试音频 | `测试音频\`（怎么用见那里的 `说明.md`） |
| 配置 | `%LOCALAPPDATA%\AutoFish\autofish.json` |
| 模板 | `%LOCALAPPDATA%\AutoFish\template.wav`（首次运行用 `--wav` 指定咬钩音后自动生成；或把 `测试音频\01_原始音效_1秒5.wav` 复制过去改名） |
| **命中录音** | `%LOCALAPPDATA%\AutoFish\hits\` |
| 判定数据 | `%LOCALAPPDATA%\AutoFish\hits.csv` |
| 运行日志 | `%LOCALAPPDATA%\AutoFish\autofish.log` |

`hits\` 里的录音是**判断归属的最终裁判**：文件名带着判定结果和分数，听一遍就知道哪次是自己的。
默认最多留 200 个。

### 每次命中都会记两样东西

只要归一分 ≥ 存在判据门限，就有两条记录 —— **判定通过没通过都会记**：

1. **`hits.csv` 一行**：那一刻的全部数值
2. **`hits\*.wav` 一段录音**：命中前后约 0.7 秒

标定期间记录门限会放宽到 0.50，所以"差一点命中"的样本也会被记下来（判定栏是 `BELOW`）。

### hits.csv 每一列是什么意思

| 列 | 含义 |
| --- | --- |
| `t` | 命中时刻 |
| `verdict` | `ACCEPT` 通过 ／ `REJECT_LEVEL` 电平不够（太小声） ／ `REJECT_LEVEL_HI` 电平太高（太响） ／ `REJECT_PAN` 声像不合格 ／ `REJECT_WINDOW` 前台窗口或光标不符 ／ `BELOW` 低于存在判据门限（只有标定期会出现） |
| `score` | 归一分 —— 存在判据 |
| `level_db` | 电平 dBFS —— 归属判据的主力 |
| `pan_db` | 声像差 dB，**0 表示正中间** —— 归属判据 |
| `rms_l_db` / `rms_r_db` | 左右声道各自的电平 |
| `coherence` | 双声道相干性（1.0 = 两个声道完全一样） |
| `centroid_hz` | 频谱质心，越高说明声音越"尖" |
| `hf_ratio` | 4 kHz 以上能量占比 |
| `lag_ms` | 在缓冲区里匹配到的位置 |
| `note` | 判定说明 |

CSV 带 BOM，**双击就能用 Excel 打开**，中文不会乱码。

### 录音文件名怎么读

```
h_20260918_164203_989_ACCEPT.wav
  └ 日期   └ 时间   └ 分数×1000(0.989)  └ 判定结果
```

默认只保留最近 200 个，超了自动删最旧的。

### 最快的打开方式

界面上点「**数据目录**」按钮，直接打开 `%LOCALAPPDATA%\AutoFish\`。


---


## 七、界面怎么看

| 区域 | 看什么 |
| --- | --- |
| 顶部 `●` 状态 | 灰 = 已停止，绿 = 挂机中，橙 = 正在执行收鱼序列 |
| 顶部 触发 / 上次 | 累计触发次数、最近一次触发的时间 |
| 设备 / 门限 / 前台 / 光标 | 四个前置条件是否就绪。显示「门限 默认值」说明还没标定过 |
| 实时指标 | 三个数值的实时值，右侧是该指标的门限 |
| 判定行 | 绿 ✔ = 这一次真的点下去了；橙 ✘ = 被哪道闸门拦住、为什么 |
| 参数 | 8 个可调数值（3 列排布），改完点「应用门限」 |
| 日志 | 每次判定一行，带分数、电平、声像差。**默认收起**，点「日志 ▾」展开 |

**门限那三个缩写**：`存` = 存在判据（只判"有没有咬钩音"），`属` = 归属判据
（判"是不是我的鱼"）。所以 `归一分` 后面是 `存 ≥ 0.80`，`电平` 和 `声像差` 后面是 `属 …`。
指标框的标题里也写了这个图例。

**声像差那条是以 0 为中心的双向条**：中间竖线是 0（正中间），条从中间向左或向右生长，
两侧的橙色小刻度就是门限位置。真正属于你的鱼应该贴着中间那一小条。

### 窗口会在挂机时自动收起并停靠到右下角

界面有两套形态，**宽度都是 560 逻辑像素**（这是"内容刚好放得下"的最小宽度）：

| | 显示什么 | 尺寸（逻辑像素 / 100% 缩放） |
| --- | --- | --- |
| **展开**（未挂机） | 监测数据 + 参数区 + 两行按钮 | **560** × 约 440 |
| **收起**（挂机中） | 只有监测数据 + 一行小按钮 | **560** × 约 270 |

之所以能压到 560：参数区排成 **3 列**（4 列放不下），按钮分成**两行**。
以后想加新按钮，记得先看看当前宽度还剩多少余量（窗口自身请求宽度约 815 物理像素，
离 840 的上限只剩 25）。

- **点「开始挂机」或按 F9 后自动收起** → 窗口缩到最小，并**停靠到屏幕右下角**，
  位置正好落在游戏右下角 HUD（弹药 / 快捷栏）的上方，不挡视野。
  点「▴ 展开面板」可以随时手动展开。
- **点「■ 停止」或再按 F9 后自动展开**回来。
- 收起态那一行按钮是：`■ 停止 (F9)` `▴ 展开面板` `日志 ▾` `↘ 贴右下角`。
- 展开态的按钮分两行：第一行 `开始挂机 (F9)` `▾ 收起面板` `日志 ▾`（常用的），
  第二行 `标定…` `应用门限` `数据目录` `退出 (F10)`（偶尔用的）。
- **停靠的锚点是窗口的右下角**：尺寸一变（收起/展开、开关日志）就重新贴一次，
  所以窗口永远朝左上方向生长，右下角钉在原地，不会长出屏幕外面。
- **宽度是定死的 560 逻辑像素**，两种形态一样宽。这个数是"内容刚好放得下"的最小值 ——
  再窄右侧的门限提示就会被裁掉（以前拖到最窄时「门限 ≥ -35.0 dBFS」只剩半截）。
  为此做了三处妥协：参数区排 3 列、按钮分两行、门限提示用 `存/属` 缩写。
- **日志区默认收起**，任何时候点「日志 ▾ / ▴」手动开合。开始挂机时会被强制收起
  （不然照样挡视野），停止后**自动恢复你原来的选择**；但如果你在挂机中自己点开过日志，
  那就以你的操作为准，展开面板时不会把它关掉。
  日志是等宽对齐的（不折行），窗口窄所以**底下有个横向滚动条**，长行往右拖就能看全。
- 收起态按钮上的文案是**跟着实际状态走**的：引擎在跑就一定写着 `■ 停止`，
  没跑就一定写着 `开始挂机`，不会出现"按钮说停、其实在启"这种事。

**想改停靠位置**：配置文件 `%LOCALAPPDATA%\AutoFish\autofish.json` 里有
`snap_margin_right` 和 `snap_margin_bottom`（逻辑像素，默认 `5` / `152`）——
分别是窗口右下角距离屏幕右边缘、下边缘的距离。改完重启即可。
`snap_margin_bottom: 152` 是按"让开游戏右下角 HUD"标定的，HUD 高度不同就调它。

> 位置是**每次启动和每次改尺寸时自动设定**的，不记忆你手动拖到哪。
> 想临时挪开就拖着用，下次收起/展开会回到右下角。


### 字体发虚的问题

之前如果你在 125% / 150% 缩放的显示器上运行，Windows 会先把窗口按 96 DPI 画成位图、
再整体拉伸 —— 结果就是所有文字都糊。现在脚本启动时会先把进程标记为 DPI-aware，
再按系统缩放比例放大所有字号和控件尺寸，文字就是原生分辨率渲染的了。

换了显示器、改了系统缩放之后，重启脚本即可，不需要改配置。

---

## 八、标定到底在做什么

### 一句话

标定 = **让脚本先空跑一段，量出"你自己的鱼"在你这个环境下是什么数值，再据此定门限。**

### 为什么不能直接给一组固定门限

三个门限全都取决于你本机的环境，我没法凭空算出来：

| 门限 | 它取决于什么 |
| --- | --- |
| 电平 ≥ −28 dBFS | 游戏内音效音量 × Windows 音量 × FxSound 增益。换个设置，同一段音效的电平能差十几 dB |
| 声像差 ≤ 5 dB | 别人一般出现在你视野的什么方位、游戏对远处音源怎么处理 |
| 归一分 ≥ 0.80 | 相对稳定（做过归一化），但受音效增强类软件影响仍会浮动 |

所以配置里那三个默认数字只是**保守的起点**，不是标定结果。

### 标定期间发生什么

跑 N 秒，走完整的检测流程，但**不发鼠标点击**（连兜底补杆也不发）。
每次命中都记下来：归一分、电平、声像差、左右声道电平、双声道相干、频谱质心，
同时切一段录音存进 `hits\`。

### 它怎么算建议值

只用一条假设：**你自己的鱼是这批命中里最响、最居中的那一个**
（你离自己的浮漂最近，而且它就在你正前方）。据此：

| 建议值 | 算法 |
| --- | --- |
| 归一分 ≥ | 观测到的 P05 再减 0.03 |
| 电平 ≥ | 电平中位数 − 6 dB |
| 电平 ≤ | 电平中位数 + 6 dB |
| 声像差 ≤ | \|声像差\| 的 P25 再加 2 dB |

> 为什么电平不再用「最高 − 12」：那个算法假设"最响的那次 = 自己的鱼"，
> 一旦标定时混进一声枪响或爆炸，最高值被顶到很响，建议下限就跟着松，
> 反而漏掉"太响"的干扰。改成**中位数为中心、上下各 6 dB**，更贴合实际
> （你自己的鱼电平基本稳定在一个值附近，中位数比"最高"抗干扰）。

### 标定的局限（很重要）

脚本**不知道哪一声是你的鱼** —— 音频里没有这个信息。所以它只能靠上面那条假设去推断，
给出的是一组"差不多能用"的起点，不是标准答案。

真正的裁判是 `hits\` 里的录音：**你自己听一遍，就能指出哪些是你的。**

推荐的闭环是：

1. 先跑一次标定，拿建议值当起点
2. 实际挂一局，把 `hits\` 里的录音听一遍
3. 听到"这条明明是别人的" → 记下它的电平/声像差 → 把门限收紧到它之外
4. 听到"这条是我的却被拦了" → 把对应门限放宽

`hits.csv` 里有每条录音对应的全部数值，录音文件名里也带着判定结果和分数，方便对照。

### 什么时候需要重新标定

- 改了游戏内的音效音量
- 改了 Windows 音量、换了音频设备、动了 FxSound 的设置
- 游戏更新之后

### 怎么跑

界面点「标定…」填秒数；或者命令行：

```
autofish.bat --calibrate 120
```

标定期间可以正常钓鱼，**需要收杆的时候自己点** —— 它只看不动手。

---

## 九、点击权限：它到底能不能点进游戏里

### 结论

能，而且**不需要任何特殊权限** —— 不需要管理员、不需要装驱动、不需要注入游戏进程。

`mouse_event` 是把左键事件注入到 **Windows 的桌面输入队列**，和物理鼠标按下走的是同一条路。
所以它不是"去改游戏"，而是"替你按了一下鼠标"。点在哪，取决于光标当时在哪。

### 实测过（跨进程）

我另起了一个**独立进程**开窗口记录点击次数，然后让脚本注入点击：

| 检查项 | 结果 |
| --- | --- |
| 脚本进程已提权 | 否 |
| 目标进程已提权 | 否 |
| 目标进程收到的点击 | **2 / 2 次** |

两个进程都是普通权限，跨进程注入成功。

### 唯一的硬门槛：UIPI

Windows 有一条规矩：**普通权限的进程不能向"以管理员身份运行"的窗口注入输入**。
而且是被**静默丢弃**的 —— 不报错、不弹窗，你只会发现"脚本在跑，但游戏里毫无反应"。

| 情况 | 结果 |
| --- | --- |
| 游戏普通权限 + 脚本普通权限 | ✅ 正常（绝大多数情况） |
| 游戏管理员 + 脚本管理员 | ✅ 正常 |
| **游戏管理员 + 脚本普通权限** | ❌ 点击被静默丢弃 |

脚本启动时会打印一行权限自检；如果检测到"游戏提权了、而脚本没有"，会在日志里明确告警。

### 实战结论：三角洲就属于这一种

2026-09-18 实测：三角洲行动的窗口标题是 `三角洲行动  `，进程是
`DeltaForceClient-Win64-Shipping.exe`，**已提权 = True**。当时脚本没提权，
于是就有了那晚的现象 —— **六次全部正确识别**（ACCEPT，分数 0.95~0.99）、
日志每次都写了「收鱼序列：抬杆 → 打断检视 → 抛竿」，**但游戏里一次都没动**：
点击全被 UIPI 静默吞掉了。

现在 `autofish.bat` 会自动请求管理员权限（弹 UAC，点「是」）。

另外脚本改用 `SendInput` 并**检查它的返回值**。按 Microsoft 的说明，被 UIPI 拦下时
`SendInput` 会返回 0 —— 所以"点了但没生效"现在是**当场发现**的硬信号，而不是靠推断。
一旦发生，日志立刻打出：

```
⚠ 点击没有生效 —— 系统没有接受注入的事件。……请用管理员身份运行 autofish.bat。
```

### 它没有做什么

澄清一下它**没有**用到的那些手段，因为那些风险高得多：

- 不注入 DLL 到游戏进程
- 不读写游戏内存
- 不装内核驱动
- 不挂系统钩子（`SetWindowsHookEx`）
- 不伪装、不绕过任何检测

它只做两件事：**监听系统音频** + **注入鼠标左键**。

最后说一句实话：`mouse_event` / `SendInput` 注入的事件，在 Windows 的低级钩子里会带上
`LLMHF_INJECTED` 标记，也就是**它本身是可以被识别为"程序合成"的**。
这一点我没打算、也没办法帮你规避。

---

## 十、音量对判定的影响

### 实测：系统音量滑块完全不影响

同一段音频，在不同系统音量下播放，测脚本判定用的「电平」：

| 系统音量 | 电平 | 归一分 |
| --- | --- | --- |
| 100% | **−25.9 dBFS** | 1.000 |
| 65% | **−25.9 dBFS** | 1.000 |
| 43% | **−25.9 dBFS** | 1.000 |
| 30% | **−25.9 dBFS** | 1.000 |
| 15% | **−25.9 dBFS** | 1.000 |

从 100% 一路拧到 15%，电平**纹丝不动**。

原因：WASAPI 回环取的是「音频引擎混音之后、端点音量之前」的信号。
主音量滑块在混音**之后**，所以回环根本抓不到它。

**所以晚上想调小音量就调，脚本的行为一模一样。** 这是好事。

### 三道判据各自的敏感度

| 判据 | 受音量影响？ | 说明 |
| --- | --- | --- |
| 归一分（存在判据） | ❌ 不受 | 归一化过；音量降 20 dB，得分仍是 1.000 |
| 声像差（归属判据） | ❌ 不受 | 左右之差，整体音量变化会相互抵消 |
| **电平（归属判据·主力）** | **看是哪个"音量"** | 见下表 |

### 关键：是哪个"音量"

| 你调的东西 | 会改变电平吗 |
| --- | --- |
| Windows **主音量滑块**（任务栏那个扬声器） | **不会** ✅ |
| 音量合成器里**「三角洲行动」那一栏** | **会** ⚠️ |
| 游戏内的**音效音量**设置 | **会** ⚠️ |
| **FxSound 的增益 / 增强强度** | **会** ⚠️ |

道理：**混音之前的任何衰减都会直接改变电平**。这一点我用测试文件验证过 ——
把音频的数字电平降 15 dB，测到的电平正好降 15 dB（−25.9 → −40.9）。
而主音量滑块在混音之后，所以不在同一条链路上。

### 实践含义

- ✅ 晚上调小音量 → **随便调**，不影响
- ⚠️ 但**别去动游戏内的音效音量**、别改音量合成器里三角洲那一栏、更别改 FxSound 的增益
- ⚠️ 一旦动了这几项 → **必须重新标定**（第九节）。否则你自己的鱼会被判成"别人的"，一次都不点

判断方法很简单：**看界面上的「电平」实时值**。如果它比平时低了一大截，就是这三项被谁动过了。




